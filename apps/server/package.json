{
  "name": "ada-maritime-observability-server",
  "version": "1.0.0",
  "description": "Observability server for Ada Maritime AI multi-agent system",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0",
    "ws": "^8.17.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.10",
    "@types/bun": "latest",
    "@types/ws": "^8.5.10"
  }
}
